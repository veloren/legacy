// Standard
use std::collections::HashMap;

// Library
//use lyon::tessellation::geometry_builder::VertexBuffers;

pub struct ResCache {
    //vbuffers: HashMap<u64, VertexBuffers>
}

impl ResCache {
    pub fn new() -> ResCache {
        ResCache {}
    }

    //pub(crate) get_or_construct_x<F: FnOnce() -> X>(hash: u64, f: F)
}
